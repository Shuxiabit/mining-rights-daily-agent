# Optional. Without a key the agent uses a deterministic offline renderer.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini

# Set to 1 to disable all outbound requests and force transparent demo data.
MINING_AGENT_OFFLINE=0
HTTP_TIMEOUT_SECONDS=12
